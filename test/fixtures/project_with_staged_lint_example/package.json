{
  "name": "staged-lint-example",
  "version": "0.3.0",
  "git-hooks": {
    "pre-commit": {
      "stagedLint": {
        "**/*.{js,ts}": [
          "bunx --bun eslint --fix",
          "bunx --bun tsc --noEmit"
        ]
      }
    }
  }
}
